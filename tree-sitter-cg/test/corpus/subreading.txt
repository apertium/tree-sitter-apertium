===========
Subreadings
===========

SUBSTITUTE SUB:1 P3 P1 TARGET Cop IF (1 Pers) ;

---

(source_file
  (rule_substitute_etc
    (ruletype_substitute_etc)
	(ruleflag (subreading))
	(inlineset (inlineset_single (setname)))
	(inlineset (inlineset_single (setname)))
	(rule_target (TARGET) (inlineset (inlineset_single (setname))))
	(IF)
	(contexttest
	  (contextpos)
	  (inlineset (inlineset_single (setname))))
    (semicolon)))

===============
all subreadings
===============

REMOVE SUB:* X IF (1 Y) ;

---

(source_file
  (rule
    (ruletype)
    (ruleflag (subreading))
    (rule_target (inlineset (inlineset_single (setname))))
    (IF)
    (contexttest (contextpos) (inlineset (inlineset_single (setname))))
    (semicolon)))

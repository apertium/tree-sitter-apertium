===========
simple test
===========

SETS

LIST X = a b c;
LIST Y = d e f;

SECTION

SELECT X IF (1 Y);

END

---

(source_file
  (section_header)
  (list
    (LIST)
    (setname)
    (eq)
    (taglist (tag (ntag)) (tag (ntag)) (tag (ntag)))
    (semicolon))
  (list
    (LIST)
    (setname)
    (eq)
    (taglist (tag (ntag)) (tag (ntag)) (tag (ntag)))
    (semicolon))
  (section_header)
  (rule
    (ruletype)
    (inlineset (inlineset_single (setname)))
    (IF)
    (contexttest
      (contextpos (ntag))
      (setname))
    (semicolon))
  (END))


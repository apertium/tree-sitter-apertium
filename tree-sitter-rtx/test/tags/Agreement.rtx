n: _.gender.number;
! <- definition.output
adj: _.gender.number;
! <- definition.output
NP: _;
! <- definition.output

gender = m f mf;
! <- definition.category
number = sg pl sp;
! <- definition.category

NP -> adj n {2 _ 1[number=2.number]};
! <- definition.reduction
!                  ^ reference.category
!                           ^ reference.category
